- id: eslint-plugin-prettier
  title: eslint-plugin-prettier phishing shipped malware
  campaigns:
  - npnjs-scam-npm-2025
  synopsis: >-
    The same npnjs.com npm maintainer phishing campaign that compromised eslint-config-prettier
    also compromised eslint-plugin-prettier.
  story: |-
    On July 18, 2025, attackers used a phished npm publishing token belonging to the maintainer JounQin to push two malicious versions of `eslint-plugin-prettier`, a popular bridge between the Prettier code formatter and the ESLint linter that pulls millions of weekly downloads across the JavaScript ecosystem. The poisoned releases, 4.2.2 and 4.2.3, were published only to the npm registry. No matching commits, pull requests, or tags appeared in the project's GitHub repository, a telltale that researchers at StepSecurity and Socket used to confirm the publishing pipeline had been hijacked rather than the source tree.

    JounQin later said he had been tricked by a phishing email pointing at `npnjs.com`, a typosquat of the legitimate npm domain that harvested his credentials and let an attacker add a fresh publishing token to his account. The same token was used the same day to ship malicious versions of several other packages JounQin maintains, including `eslint-config-prettier`, `synckit`, `@pkgr/core`, and `napi-postinstall`.

    Socket described the payload as a Windows-only loader. On install, the package checked the platform and, on Windows, spawned `rundll32` against a bundled `node-gyp.dll`, a name chosen to blend in with the legitimate `node-gyp` build tool. The DLL was later cataloged as part of the Scavenger infostealer family and tracked as CVE-2025-54313.

    JounQin revoked the leaked token, deprecated the malicious versions, and worked with npm support to remove them. Downstream consumers who installed during the window were advised to treat any credentials reachable from the affected machine, including npm tokens, browser secrets, and SSH keys, as compromised.
  start_date: 2025-07-18
  end_date: 2025-07-19
  target:
    name: eslint-plugin-prettier
    kind: library
    repo: https://github.com/prettier/eslint-plugin-prettier
  actor:
    name: Third Party
    kind: group
  language: JavaScript
  method:
    cause: social_engineering
    phase: distribution
    motive: remote_code_execution
    transitive: true
  impact:
    types:
    - code_execution
    users: 0
  artifacts:
  - id: eslint-plugin-prettier
    name: eslint-plugin-prettier
    ecosystem: npm
    kind: source_archive
    repo: https://github.com/prettier/eslint-plugin-prettier
    start_date: 2025-07-18
    end_date: 2025-07-19
    versions:
    - 4.2.2
    - 4.2.3
    fixed_versions:
    - 4.2.4
    - 4.2.5
    locations:
    - uri: https://www.npmjs.com/package/eslint-plugin-prettier/v/4.2.2
      role: distribution
    - uri: https://www.npmjs.com/package/eslint-plugin-prettier/v/4.2.3
      role: distribution
    - uri: https://www.npmjs.com/package/eslint-plugin-prettier
      role: mirror
    indicators:
    - kind: file
      value: node-gyp.dll
    - kind: process
      value: rundll32
    - kind: file_sha256
      value: install.js 32d0dbdfef0e5520ba96a2673244267e204b94a49716ea13bf635fa9af6f66bf
    - kind: file_sha256
      value: node-gyp.dll c68e42f416f482d43653f36cd14384270b54b68d6496a8e34ce887687de5b441
  references:
  - url: 
      https://www.stepsecurity.io/blog/supply-chain-security-alert-eslint-config-prettier-package-shows-signs-of-compromise
    title: eslint-config-prettier package shows signs of compromise
  - url: 
      https://socket.dev/blog/npm-phishing-campaign-leads-to-prettier-tooling-packages-compromise
    title: npm phishing campaign leads to Prettier tooling packages compromise
  - url: https://www.crowdstrike.com/en-us/blog/crowdstrike-falcon-prevents-npm-package-supply-chain-attacks/
    title: CrowdStrike Falcon Prevents Supply Chain Attack Involving Compromised NPM Packages
