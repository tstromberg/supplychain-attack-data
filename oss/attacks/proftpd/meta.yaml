- id: proftpd
  title: ProFTPD site served backdoored source
  synopsis: >-
    ProFTPD's main FTP and rsync distribution server was compromised on November 28, 2010,
    and the 1.3.3c source archives were replaced with backdoored copies until December 2.
  story: |
    Around 20:00 UTC on November 28, 2010, attackers compromised ProFTPD's main distribution server, `ftp.proftpd.org`, which also acted as the project's rsync source for official mirrors. They replaced ProFTPD 1.3.3c source archives with modified copies, so anyone downloading from the official site or mirrors through December 2 could receive poisoned source through the normal project channel.

    Delivery happened twice: first through the source archive, then through the build. The modified `configure` path compiled `tests/tests.c` and ran it, so a builder could call out before a daemon was installed. The runtime daemon carried a separate patch in `src/help.c`.

    The backdoor had two faces. At build time, the modified `configure` script compiled and ran `tests/tests.c`, which attempted an HTTP callback to 212.26.42.47 on port 9090. At runtime, a patch in `src/help.c` gave unauthenticated users root shell execution when they issued `HELP ACIDBITCHEZ`. The published diff and hashes turned the incident into a compact lesson in source-release trust: a project server, a mirror network, and a small source patch were enough to turn a widely deployed FTP daemon into a root shell.

    ProFTPD's response was to tell users to verify checksums and replace any 1.3.3c source obtained during the exposure window. That is the practical cleanup burden in source-distribution attacks: downstream packagers and administrators have to ask not only which version they built, but which copy of that version they built from.
  pull_quote:
    text: >-
      The ProFTPD core team believed the way into `ftp.proftpd.org` was a
      ProFTPD vulnerability that had already been announced and fixed
      upstream, but sat unpatched on the project's own distribution server.
    caption: the fix shipped, the server stayed unpatched
    after: 2
  start_date: 2010-11-28
  end_date: 2010-12-02
  target:
    name: proftpd
    kind: application
    website: https://ftp.proftpd.org
    repo: https://github.com/proftpd/proftpd
  actor:
    name: Unknown attacker
    kind: unknown
  language: C
  method:
    cause: compromised_infrastructure
    phase: distribution
    motive: unauthorized_access_control
    transitive: false
  impact:
    types:
    - backdoor
    - remote_code_execution
  artifacts:
  - id: proftpd
    name: proftpd
    kind: source_archive
    website: https://ftp.proftpd.org
    repo: https://github.com/proftpd/proftpd
    start_date: 2010-11-28
    end_date: 2010-12-02
    versions:
    - 1.3.3c
    locations:
    - uri: ftp://ftp.proftpd.org/distrib/source/proftpd-1.3.3c.tar.gz
      role: distribution
    - uri: ftp://mirror.proftpd.org/proftpd-1.3.3c.tar.gz
      role: distribution
    - uri: https://www.exploit-db.com/application/15662
      role: mirror
    - uri: https://www.aldeid.com/wiki/Exploits/proftpd-1.3.3c-backdoor
      role: mirror
    - uri: https://www.theregister.com/2010/12/02/proftpd_backdoored/
      role: mirror
    - uri: 
        https://www.zdnet.com/article/open-source-proftpd-hacked-backdoor-planted-in-source-code/
      role: mirror
    hashes:
    - md5:a43df54cc0b16c9e63a1045129d7b144
    - sha256:d56d6d643534fe618b26807948b3cfe43c02b3f7abf7f4a073778c9c1666d1eb
    indicators:
    - kind: observable
      value: ftp://ftp.proftpd.org/distrib/source/proftpd-1.3.3c.tar.gz
    - kind: observable
      value: ftp://mirror.proftpd.org/proftpd-1.3.3c.tar.gz
    - kind: observable
      value: HELP ACIDBITCHEZ
    - kind: observable
      value: 212.26.42.47:9090
    - kind: file
      value: src/help.c
    - kind: file
      value: tests/tests.c
    evidence:
    - kind: source_archive
      path: samples/proftpd/before/proftpd-1.3.3c.tar.bz2
      role: clean_same_version_comparator
    - kind: source_diff
      path: samples/proftpd/during/proftpd-1.3.3c-rootkit.patch
      role: malicious_changes
    notes:
    - >-
      The previously listed MD5 values 4f2c554d6273b8145095837913ba9e5d
      and 8571bd78874b557e98480ed48e2df1d2 identify the clean gzip and bzip2
      archives respectively; they are not malicious artifact hashes.
    - >-
      SecurityFocus BID 45150 was previously listed as a location. The BID database
      is retired and the URL now serves a generic SecurityFocus notice with no entry
      for this incident, so the location was removed.
  references:
  - url: https://www.theregister.com/2010/12/02/proftpd_backdoored/
    title: Hackers poison well of open-source FTP app
  - url: 
      https://www.zdnet.com/article/open-source-proftpd-hacked-backdoor-planted-in-source-code/
    title: Open source ProFTPD hacked, backdoor planted in source code
  - url: https://www.aldeid.com/wiki/Exploits/proftpd-1.3.3c-backdoor
    title: Exploits/proftpd-1.3.3c-backdoor
  - url: https://lwn.net/Articles/418478/
    title: Back door in ProFTPD FTP server (The H)
  - url: https://sourceforge.net/p/proftp/mailman/proftp-user/thread/alpine.LSU.2.02.1012011556230.25361%40schiller.dip.t-dialin.net/
    title: Distribution file rootkit-compromised - ProFTPD user mailing list
