- id: kubernetes-el
  title: kubernetes-el workflow poisoned its repo
  synopsis: >-
    A Pwn Request flaw let attacker-controlled PR code run with kubernetes-el repository privileges.
    The stolen token defaced the repo and replaced kubernetes.el with a destructive shell command.
  story: |
    In early March 2026, an attacker using the GitHub handle `quicktrinny` opened what looked like an ordinary pull request against kubernetes-el, a long-running Emacs interface for Kubernetes, and within hours had replaced the project's main source file with an Emacs expression that called `rm -rf /`. The compromise was disclosed by Emacsmirror maintainer Jonas Bernoulli on March 7.

    The opening was a Pwn Request. kubernetes-el's CI fired on `pull_request_target`, the GitHub Actions trigger that grants repository-write permissions, and then explicitly checked out the pull request head. That combination, which StepSecurity has flagged repeatedly, let the attacker's fork code run with the project's own credentials. The payload then evolved live in the workflow logs. The attacker first changed the Makefile to execute a script called `funny.sh`, then experimented with runner memory dumping, swapped tools, fixed build errors, and finally exfiltrated GitHub Actions secrets to a `webhook.site` endpoint. The valuable secret turned out to be the workflow's own writable `GITHUB_TOKEN`.

    With that token in hand, the attacker pushed commits authored as `github-actions[bot]`. The README was defaced and `kubernetes.el` itself was rewritten so that loading the package would invoke `rm -rf /`. For Emacs users who installed or updated the package directly from the repository, source code had become the delivery channel.

    MELPA pulled the package and Emacsmirror blocked further updates, holding the damage to a narrow window. StepSecurity, which wrote up the incident, used it as a reminder that `pull_request_target` plus a fork-head checkout remains one of the most reliable ways to hand a repository to a stranger.
  start_date: 2026-03-05T00:00:00.000Z
  end_date: 2026-03-07T00:00:00.000Z
  target:
    name: kubernetes-el
    kind: library
    website: https://github.com
    repo: https://github.com/kubernetes-el/kubernetes-el
  actor:
    name: Unknown
    kind: unknown
  language: Emacs Lisp
  method:
    cause: gha_vulnerability
    phase: source
    motive: sabotage_credential_theft
    transitive: false
  impact:
    types:
      - destructive_code_execution
      - credential_theft
    users: 0
  artifacts:
    - id: kubernetes-el
      name: kubernetes-el
      ecosystem: github
      kind: source_repository
      website: https://github.com
      repo: https://github.com/kubernetes-el/kubernetes-el
      start_date: 2026-03-05T00:00:00.000Z
      end_date: 2026-03-07T00:00:00.000Z
      hashes:
        - sha1:e9119ac7b801981b2f8a5a14ac5a64547feb6b93
      locations:
        - uri: https://github.com/kubernetes-el/kubernetes-el/pull/382
          role: distribution
        - uri: https://github.com/kubernetes-el/kubernetes-el/actions/runs/22702282382
          role: distribution
        - uri: https://github.com/kubernetes-el/kubernetes-el/actions/runs/22702314529
          role: distribution
        - uri: https://github.com/kubernetes-el/kubernetes-el/commit/929c639
          role: distribution
        - uri: https://github.com/kubernetes-el/kubernetes-el/commit/09e06af
          role: distribution
        - uri: https://github.com/kubernetes-el/kubernetes-el/issues/383
          role: distribution
        - uri: https://github.com/kubernetes-el/kubernetes-el
          role: mirror
      commits:
        - e9119ac7b801981b2f8a5a14ac5a64547feb6b93
      indicators:
        - kind: account
          value: quicktrinny
        - kind: file
          value: funny.sh
        - kind: domain
          value: webhook.site
        - kind: file
          value: kubernetes.el
        - kind: observable
          value: >-
            github-actions[bot] commits defaced README and replaced kubernetes.el with destructive
            shell execution.
      notes:
        - >-
          Affected revision e9119ac7b801981b2f8a5a14ac5a64547feb6b93, which identifies a commit
          rather than a released version.
  references:
    - url: >-
        https://www.stepsecurity.io/blog/kubernetes-el-compromised-how-a-pwn-request-exploited-a-popular-emacs-package
      title: 'kubernetes-el Compromised: How a Pwn Request Exploited a Popular Emacs Package'
    - url: https://github.com/kubernetes-el/kubernetes-el/issues/383
      title: Repository compromised via GitHub Actions token
    - url: https://github.com/NixOS/nixpkgs/issues/498290
      title: 'nixpkgs issue #498290 - Repo for kubernetes-el has been compromised'
