---
- id: shai-hulud-wot-api-npm
  title: wot-api npm package carried Shai-Hulud
  campaigns:
  - shai-hulud-here-we-go-again
  synopsis: JFrog listed 1 wot-api npm package in the May 2026 Shai-Hulud wave. This
    record scopes those artifacts to their own official distribution surface.
  story: |-
    wot-api was a single-package entry in TeamPCP's May 2026 Shai-Hulud wave. JFrog listed four affected npm releases under the `wot-api` package name during the May 11-12 exposure window.

    The compromise followed the campaign's registry-first model. A normal package install was enough to create risk if it ran in a developer shell, CI runner, or build container with tokens and configuration nearby. The malware's value came from harvesting those credentials and using them to publish additional infected packages.

    This record keeps `wot-api` separate from the larger campaign rollup because package-level evidence is what defenders can search. The campaign page explains the shared TeamPCP loader, infrastructure, and propagation behavior; this page preserves the affected versions and npm distribution paths.

    Cleanup should follow every place the affected `wot-api` releases resolved: lockfiles, caches, package mirrors, build images, and CI logs. Any matching environment should be reviewed for credential theft and downstream publishing activity before it is trusted again.
  start_date: '2026-05-11'
  end_date: '2026-05-12'
  target:
    name: wot-api
    kind: library
  actor:
    name: TeamPCP
    kind: group
  language: JavaScript
  method:
    cause: compromised_account_credentials
    phase: distribution
    motive: credential_theft
    transitive: true
  impact:
    types:
    - credential_theft
    - self_propagation
    users: 0
  notes:
  - Minimal campaign-linked record created to keep Shai-Hulud package evidence scoped
    by vendor, organization, maintainer account, or package distribution surface.
  artifacts:
  - id: wot-api
    name: wot-api
    package: wot-api
    ecosystem: npm
    kind: source_archive
    start_date: '2026-05-11'
    end_date: '2026-05-12'
    versions:
    - 0.8.3
    - 0.8.4
    - 0.8.2
    - 0.8.1
    locations:
    - uri: https://www.npmjs.com/package/wot-api/v/0.8.3
      role: distribution
    - uri: https://www.npmjs.com/package/wot-api/v/0.8.4
      role: distribution
    - uri: https://www.npmjs.com/package/wot-api/v/0.8.2
      role: distribution
    - uri: https://www.npmjs.com/package/wot-api/v/0.8.1
      role: distribution
  references:
  - url: https://research.jfrog.com/post/shai-hulud-here-we-go-again/
    title: 'Shai-Hulud: Here We Go Again - Worm by TeamPCP Hits NPM and PyPI'
